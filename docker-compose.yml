# Use root/example as user/password credentials
version: '3.7'

services:

  mongo:
    image: mongo
    ports:
        - 27017:27017
    restart: always
    environment:
      MONGO_INITDB_DATABASE: parse-server-webplugin